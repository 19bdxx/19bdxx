# 第二章 风电 SCADA 数据质量提升——博士论文写作详细报告

> **文档定位**：本报告面向博士论文第二章的撰写，详细说明各小节的写作目的、核心内容、数据支撑、方法框架、实验设计以及与全文逻辑链的衔接关系，可直接作为章节写作大纲使用。
>
> **章节总目标**：系统解决风电 SCADA 数据中存在的层级差异、重复时间戳、异常数据和空气密度效应四类质量问题，为后续第三、四章的建模提供高质量、可信赖的数据基础。
>
> **更新日期**：2026-04

---

## 目录

1. [章节引言（2.0）](#20-章节引言)
2. [第 2.1 节：风电 SCADA 数据多层级功率结构分析](#21-风电-scada-数据多层级功率结构分析)
3. [第 2.2 节：重复时间戳成因分析与处理方法](#22-重复时间戳成因分析与处理方法)
4. [第 2.3 节：基于热力图的功率曲线异常模式识别](#23-基于热力图的功率曲线异常模式识别)
5. [第 2.4 节：考虑空气密度的功率曲线数据精细清洗](#24-考虑空气密度的功率曲线数据精细清洗)
6. [第 2.5 节：本章小结](#25-本章小结)
7. [附录：写作建议与章节衔接说明](#附录写作建议与章节衔接说明)

---

## 2.0 章节引言

### 写作目的

引言部分完成三项任务：
1. **问题引入**：说明 SCADA 数据质量差是制约风电功率预测精度的根本原因之一；
2. **现状交代**：简要指出现有研究在数据质量处理方面的不足（为第一章文献综述做补充）；
3. **本章结构说明**：用一段话或一张流程图交代本章四个小节的逻辑关系。

### 核心内容

**（1）SCADA 数据质量问题的工程背景**

风电场 SCADA（数据采集与监视控制）系统以 10 分钟为基本统计周期，采集风机层、集电线路层和场站层的多维度运行参数（风速、功率、温度、偏航角等）。由于采集设备精度差异、通信链路不稳定、运行工况复杂等原因，原始 SCADA 数据普遍存在以下问题：

| 质量问题类型 | 典型表现 | 对预测建模的影响 |
|------------|---------|---------------|
| 层级功率差异 | 风机功率之和 ≠ 全站并网功率 | 预测目标定义混乱，评估基准不统一 |
| 重复时间戳 | 同一时刻存在多条记录 | 时序分析失效，模型输入维度错误 |
| 功率曲线异常 | 限电截顶、结冰失速、传感器漂移等 | 功率曲线建模偏差，预测系统性偏高或偏低 |
| 空气密度效应 | 夏冬季功率曲线系统性偏移 | 季节性预测误差，跨季泛化性差 |

**（2）本章研究逻辑框架**

```
原始 SCADA 数据
       │
       ▼
§2.1 多层级功率结构分析
   ─ 量化风机/集电线路/全站三层级功率差异
   ─ 确定"正值保留"策略与分析口径
       │ 层级清晰的数据口径
       ▼
§2.2 重复时间戳成因分析与处理
   ─ 识别并消除时间序列歧义
       │ 时间轴唯一、完整的数据
       ▼
§2.3 热力图异常模式识别（粗识别）
   ─ 可视化定位四类异常区域
   ─ 提供精细清洗的"目标区域先验"
       │ 异常区域标记
       ▼
§2.4 考虑空气密度的精细清洗（精清洗）
   ─ 以风速+空气密度为双特征 KNN 剔除异常
   ─ 消除季节性密度效应
       │
       ▼
高质量功率曲线数据（输出给第三、四章）
```

---

## 2.1 风电 SCADA 数据多层级功率结构分析

### 写作目的

本节的核心作用有两个：一是**建立数据认知基础**，厘清风机层、集电线路层和场站层功率数据的来源与差异，防止后续建模混用不同口径数据；二是**为第三章分层预测提供物理知识**，量化线损规律，为线损修正模型提供先验数据支撑。

---

### 2.1.1 风电场电气拓扑与 SCADA 计量体系

**写作内容：**

介绍风电场的标准电气拓扑结构，说明 SCADA 计量体系的三层级结构：

```
风机层：         [WTG₁] [WTG₂] ... [WTGₙ]
                   ↓       ↓           ↓
                   有功功率（机端计量，含内部损耗）

集电线路层：    [集电线路 L₁] [集电线路 L₂] ... [集电线路 Lₖ]
                   ↓               ↓                    ↓
                   集电线路有功（线路末端/汇流箱处计量）

场站层：        [升压变压器]
                   ↓
                   全站并网有功功率（关口计量，电网结算口径）
```

**具体写作要点：**
- 各层级计量装置的安装位置（SCADA 传感器点位图）
- 计量数据的物理含义差异（机端 vs. 关口）
- 多场站数据描述：峡阳A、峡阳B、峡沙、蕴阳等风电场的基本参数（装机容量、风机台数、集电线路数）

---

### 2.1.2 三层级功率差异的构成与量化

**写作内容：**

系统分析造成三层级功率差异的三类来源，并在真实数据上量化每类来源的贡献：

**（1）集电线路有功线损**

线损的物理来源为集电电缆的欧姆电阻：
$$\Delta P_{\text{line}} = I^2 R = \frac{P^2 + Q^2}{U^2} \cdot R$$

其中 $P$ 为传输有功功率，$Q$ 为无功功率，$U$ 为线路电压，$R$ 为线路电阻。线损随传输功率的增大而非线性增大，在风机满发时线损最大。

**（2）厂用电消耗**

场站自身运行消耗的电量（变压器铁损、铜损、风机控制系统用电、辅助设备用电等），通常通过关口电表与风机电表之差体现。

**（3）计量位置差异引起的系统性偏差**

部分风场的风机功率计量点与集电线路计量点之间存在一段电缆，其线损被计入风机侧而非集电线路侧，导致"风机功率合计 > 集电线路功率合计"。

**实验设计：**
- 选取多场站（至少4个）全年 10 分钟分辨率数据
- 计算各场站三层级功率差值的统计特征（均值、标准差、分位数）
- 绘制**不同功率水平下的线损率分布图**（箱线图 + 散点图）
- 建立线损率与功率区间的查表关系（为第三章 §3.3 提供先验）

---

### 2.1.3 负功率数据的成因与处理策略

**写作内容：**

风机在停机状态下会消耗少量厂用电（加热系统、偏航系统待机等），导致 SCADA 记录出现负有功功率。部分场站的汇总数据也因相同原因出现负值。现有研究处理策略不一，本节从物理机制出发论证"正值保留"策略的合理性：

| 处理策略 | 做法 | 优点 | 缺点 |
|---------|------|------|------|
| 直接截断为零 | 将负值替换为 0 | 简单 | 掩盖停机功耗信息，统计特征失真 |
| 正值保留 | 仅保留有功功率 > 0 的时间步 | 聚焦发电状态，统计口径清晰 | 丢失停机期间数据 |
| 区间筛选 | 同时过滤有效风速区间（如 3~25 m/s）| 兼顾发电状态与有效工况 | 需确定切入/切出风速阈值 |

**论证重点**：对于"功率曲线建模"和"功率预测"任务，"正值保留"策略聚焦于机组实际发电状态，在此基础上的统计建模更稳健；同时通过实验验证三种策略对最终功率曲线 RMSE 的影响，客观呈现差异。

---

### 2.1.4 本节小结

总结三层级功率差异的量化结论，给出后续章节数据使用的统一口径规范（如：第三章使用"机端功率→集电线路功率（含线损修正）→全站功率"的三层级数据；第四章直接使用全站并网功率作为预测目标）。

---

## 2.2 重复时间戳成因分析与处理方法

### 写作目的

重复时间戳是 SCADA 数据中普遍存在但研究较少的质量问题。本节的目标是：
1. 从 SCADA 系统的**采集机制**出发，分类揭示重复时间戳的产生原因（不同于以往研究仅将其作为异常直接丢弃）；
2. 提出基于**物理合理性判别**的分类去重策略，保留有效信息的同时消除时间轴歧义；
3. 定量评估不同去重方案对下游功率曲线建模精度的影响。

---

### 2.2.1 SCADA 系统采集机制与时间戳生成原理

**写作内容：**

- **SCADA 采集周期**：典型风电场 SCADA 以 1 秒或 10 秒为原始采样周期，以 10 分钟为统计汇总周期。时间戳记录的是统计区间的**结束时刻**（或开始时刻，各厂商不同），需明确标注。
- **时钟同步机制**：SCADA 主站通过 NTP（网络时间协议）或 GPS 授时对子站时钟进行同步。当同步失败或网络延迟较大时，子站时钟可能发生漂移，导致相邻统计周期的时间戳出现重叠或跳变。
- **数据库写入机制**：主站数据库存在"断点续传"机制，当通信中断恢复后，子站会重传断线期间的历史数据，与已入库的数据形成重复记录。

**插图建议**：SCADA 系统架构示意图（风机 RTU → 子站 → 主站数据库）+ 时钟漂移与重复时间戳产生示意图。

---

### 2.2.2 重复时间戳的类型分类

**写作内容：**

通过对实际数据的深入分析，将重复时间戳归纳为以下四类：

| 类型 | 描述 | 典型特征 | 建议处理 |
|------|------|---------|---------|
| **完全重复型** | 同一时刻存在两条记录，所有字段完全一致 | 各变量值完全相同 | 直接去重，保留任意一条 |
| **断点续传型** | 通信恢复后重传历史数据，与正常数据重叠 | 时间戳重复，值一致或差异极小 | 保留原始入库记录，删除重传记录 |
| **时钟漂移型** | 子站时钟漂移导致统计周期划分错误，相邻周期时间戳相同 | 时间戳重复，功率值存在小幅差异 | 按物理合理性保留较接近滑动均值的记录 |
| **多系统汇聚型** | 同一风机数据由多套系统（如 AGC + SCADA）并行采集，在数据库汇聚时形成重复 | 时间戳相同，部分字段值差异较大 | 以主站数据为准，删除副系统记录 |

---

### 2.2.3 分类去重策略与效果评估

**写作内容：**

**去重算法框架：**

```
输入：原始 SCADA 时序数据
  ↓
Step 1：识别重复时间戳（groupby 时间戳，count > 1）
  ↓
Step 2：对每组重复记录，计算各记录的字段差异程度
        - 所有字段差异 < ε  →  完全重复型  →  直接去重
        - 功率差异 < 阈值 且 时间连续  →  断点续传型 / 时钟漂移型
        - 功率差异 > 阈值  →  多系统汇聚型  →  按数据源优先级保留
  ↓
Step 3：对保留记录进行时序完整性验证（等间隔检查）
  ↓
输出：时间轴唯一、等间隔的 SCADA 数据
```

**评估指标：**
- 各类型重复记录的占比（不同风场对比）
- 不同去重策略对功率曲线 RMSE 的影响（策略对比实验）
- 去重前后的数据完整率变化

---

### 2.2.4 缺失时间步的识别与处理

**写作内容：**

去重后可能出现时间步缺失（断线期间数据缺失）。本节简要介绍缺失值的识别方法和填充策略：

- **短时缺失**（< 3 个连续时间步）：线性插值；
- **中时缺失**（3~12 个时间步）：基于相邻风速-功率关系的物理约束插值；
- **长时缺失**（> 12 个时间步，对应停机或检修）：标记并剔除，不参与建模。

注：本节聚焦于缺失的识别与标记策略，插值细节不作为本节重点（可作为附录）。

---

### 2.2.5 本节小结

给出各类型重复时间戳在多场站数据中的占比统计（建议制表），总结分类去重策略的有效性，说明经本节处理后时序数据的完整性状态，为后续功率曲线分析奠定基础。

---

## 2.3 基于热力图的功率曲线异常模式识别

### 写作目的

本节提出一套以**风速-功率二维密度热力图**为核心的异常识别框架，实现对功率曲线中多类异常模式的**自动化分类识别**。本节在第二章中扮演"粗识别"角色，其输出结果（异常区域的位置与模式类型）作为 §2.4 精细清洗的先验信息。

---

### 2.3.1 功率曲线异常类型的分类体系

**写作内容：**

系统梳理风电 SCADA 数据中功率曲线的主要异常模式，建立一套完整的**异常模式分类体系**：

| 异常模式 | 物理成因 | 热力图表现 | 影响 |
|---------|---------|-----------|------|
| **限电截顶** | 电网调度指令限制出力 | 中高风速区功率存在明显水平截断线，密度集中于额定值以下某固定水平 | 低估风机实际出力能力，功率曲线平台段失真 |
| **结冰失速** | 叶片结冰导致气动性能下降 | 中低风速区出现低于正常功率曲线的散点族，密度向下偏移 | 系统性低估发电量，引入季节性偏差 |
| **传感器漂移** | 风速仪老化或校准偏差 | 功率曲线整体左移或右移（风速系统偏高或偏低），S型曲线形态扭曲 | 风速-功率关系映射错误，模型输入特征失真 |
| **停机拉尾** | 停机/维修期间数据未被剔除，低风速对应零功率 | 低风速区沿 x 轴（零功率线）出现长尾密度分布 | 扩大低风速区的功率预测不确定度 |
| **尾流遮挡** | 上游机组尾流造成下游来流衰减 | 对于某些风向，中风速区功率密度向左偏移（来流风速被低估） | 风向相关的系统性偏差，常被误作传感器漂移 |
| **电气故障截断** | 电缆/变压器故障导致集电线路局部停运 | 高风速区突然出现大量零功率点 | 功率曲线高风速端密度稀疏，建模样本不足 |

**插图建议**：针对上述各异常模式，各绘制一张典型的风速-功率热力图，并标注异常区域（可用矩形框或等密度轮廓线标注）。

---

### 2.3.2 风速-功率二维密度热力图的构建方法

**写作内容：**

**（1）热力图构建流程**

```
输入：经 §2.2 处理的等间隔 SCADA 数据 {(v_i, P_i)}
  ↓
Step 1：确定风速轴分辨率（推荐 0.1 m/s 或 0.5 m/s 分箱）
         功率轴分辨率（推荐以额定功率百分比分箱，如 1%）
  ↓
Step 2：二维核密度估计（KDE）或直方图统计
         - KDE：使用高斯核，带宽通过 Silverman 法则或交叉验证选取
         - 直方图：按风速-功率网格计数，归一化后得到联合概率密度
  ↓
Step 3：可视化（热图颜色映射推荐使用对数归一化以突出低密度区域）
  ↓
输出：二维密度热力图 + 密度矩阵（供后续自动识别使用）
```

**（2）可视化参数选择**

- **颜色映射**：推荐 `viridis` 或 `plasma`（避免彩虹色）；
- **对数归一化**：使用 `LogNorm` 处理密度跨度大的问题（高密度区不过饱和，低密度区仍可见）；
- **叠加理论功率曲线**：在热力图上叠加标准功率曲线（厂商保证曲线或标准 IEC 方法拟合曲线），直观显示偏差。

---

### 2.3.3 基于热力图的异常区域自动识别算法

**写作内容：**

将热力图从"目视工具"升级为**自动化识别框架**，提出以下识别规则：

**（1）限电截顶识别**

- **特征**：在额定功率附近某固定功率水平出现水平高密度条带；
- **识别算法**：计算各功率分箱（纵轴）的点数分布，若某功率水平附近出现明显密度峰值（且该峰值不在额定功率处），标记为限电截顶区域；
- **参数**：限电功率阈值 $P_{\text{curtail}}$（通过峰值检测自动确定）。

**（2）结冰失速识别**

- **特征**：中低风速区存在一组功率值明显低于正常曲线的散点族；
- **识别算法**：以分箱统计的 10% 分位线作为正常功率下界，将密度中心低于下界 20% 以上的风速区间标记为结冰失速候选区；
- **时序验证**：结冰通常发生在连续时间段，可通过时序滑窗验证。

**（3）传感器漂移识别**

- **特征**：整条功率曲线相对于标准曲线发生系统性水平偏移；
- **识别算法**：以各风速分箱的中位功率值拟合功率曲线，计算与标准曲线的偏差（DTW 距离或分段 RMSE），若全局偏差超过阈值，标记为传感器漂移。

**（4）停机拉尾识别**

- **特征**：低风速区（0~4 m/s）存在大量零功率或接近零功率的数据点；
- **识别算法**：统计低风速区功率为零的点占比，超过阈值则标记；进一步通过功率变化率分析区分"切入前"和"停机"状态。

**（5）异常区域可视化输出**

在热力图上叠加各类异常区域的标注（矩形框、颜色区域或等密度轮廓），生成带标注的异常诊断热力图，作为 §2.4 清洗算法的输入先验。

---

### 2.3.4 与传统方法的对比实验

**写作内容：**

将热力图异常识别框架与以下传统方法进行对比：

| 对比方法 | 核心思路 | 局限性（在多异常场景下） |
|---------|---------|----------------------|
| IEC 61400-12-1 分箱法 | 每个风速箱内按 nσ 剔除 | 仅处理单一异常，无法区分限电和结冰 |
| 3σ / IQR 全局统计 | 全局分布的统计边界 | 对多峰分布误识别严重 |
| 孤立森林 | 全局高维异常检测 | 无法区分异常类型，可解释性差 |
| **本文热力图方法** | 二维密度 + 规则识别 | — |

**实验指标**：
- 异常数据识别的准确率（Precision）、召回率（Recall）和 F1 分数（需人工标注少量样本作为金标准）；
- 各方法在单异常和多异常并存场景下的性能对比。

---

### 2.3.5 本节小结

总结热力图异常识别框架的特点和局限，说明识别结果（各类异常的区域标记）如何传递给 §2.4 的精细清洗，形成"粗识别→精清洗"的两步流程。

---

## 2.4 考虑空气密度的功率曲线数据精细清洗

### 写作目的

在 §2.3 异常区域先验的基础上，本节针对功率曲线数据中两类关键问题提出系统的**精细清洗方法**：
1. **空气密度效应**：空气密度的季节性变化导致功率曲线发生系统性偏移，若不修正则引入季节性预测误差；
2. **局部异常数据**：利用 KNN 在风速-空气密度二维特征空间中精细剔除偏离正常分布的数据点。

---

### 2.4.1 空气密度对功率曲线的影响机理

**写作内容：**

**（1）物理原理**

风能密度（单位面积上的风功率）与空气密度成正比：
$$P_{\text{wind}} = \frac{1}{2} \rho A v^3$$

其中 $\rho$ 为空气密度（kg/m³），$A$ 为风轮扫掠面积（m²），$v$ 为风速（m/s）。对于同样的风速，空气密度越高，风机可提取的能量越多，输出功率越大。

**（2）空气密度的计算方法**

依据气象数据（干球温度 $T$、大气压力 $P$、相对湿度 $RH$）计算实际空气密度：
$$\rho = \frac{P}{R_d T} \left(1 - \frac{e_s \cdot RH/100}{P}\left(1 - \frac{R_d}{R_v}\right)\right)$$

其中 $R_d = 287.05$ J/(kg·K) 为干空气比气体常数，$R_v = 461.5$ J/(kg·K) 为水蒸气比气体常数，$e_s$ 为饱和水蒸气压（由 Magnus 公式或 Buck 方程计算）。

实际工程中常用简化公式（当湿度数据不可用时）：
$$\rho \approx \frac{P}{R_d T}$$

**（3）季节性密度变化的量化**

以典型风电场为例，展示全年 10 分钟数据中空气密度的季节性变化：
- 夏季（气温高、气压低）：ρ 约 1.05~1.10 kg/m³；
- 冬季（气温低、气压高）：ρ 约 1.25~1.35 kg/m³；
- 季节差异可达 15%~20%，对功率曲线偏移影响显著。

**插图建议**：同一机组全年数据按季节着色的风速-功率散点图，直观展示季节性密度效应。

---

### 2.4.2 IEC 标准修正方法及其局限性

**写作内容：**

**（1）IEC 61400-12-1 修正风速方法**

IEC 标准采用"修正风速"消除密度差异：
$$v_{\text{corr}} = v \cdot \left(\frac{\rho}{\rho_0}\right)^{1/3}$$

其中 $\rho_0 = 1.225$ kg/m³ 为标准大气密度。将 $v_{\text{corr}}$ 替换 $v$ 后建立"密度无关"的标准功率曲线。

**（2）局限性分析**

- 该修正假设风机叶片始终处于最优叶尖速比工作点，对于变速变桨机组在额定功率以下区域适用性较好，但在额定功率以上的截断区域（机组已满发）不适用；
- 对于高海拔地区（如我国西北高原，平均 ρ ≈ 0.95~1.05 kg/m³），修正后的功率曲线仍与低海拔地区存在差异；
- 简单的风速修正无法在**异常检测阶段**利用密度信息，而只是在拟合阶段进行校正。

**本节的创新点**：将空气密度不仅用于"修正"，还作为**异常检测的特征维度**，直接在二维（风速, 空气密度）特征空间中进行 KNN 邻域异常检测。

---

### 2.4.3 基于 KNN 的二维特征空间精细清洗算法

**写作内容：**

**（1）特征空间构建**

以**修正风速** $v_{\text{corr}}$ 和**空气密度** $\rho$（或以 $\rho/\rho_0$ 归一化）为两个特征维度，构建二维特征空间：
$$\mathbf{x}_i = (v_{\text{corr},i},\ \rho_i)$$

对每个样本，还有对应的功率 $P_i$ 作为清洗的判断依据。

**（2）KNN 局部异常检测算法**

对于每个数据点 $(v_{\text{corr},i}, \rho_i, P_i)$：

```
Step 1：在 (v_corr, ρ) 二维空间中，找到第 i 个点的 K 个最近邻
         （欧氏距离，特征需先归一化到 [0,1]）

Step 2：计算 K 个近邻的功率均值 μ_i 和标准差 σ_i

Step 3：若 |P_i - μ_i| > n·σ_i，则将第 i 个点标记为异常
         （n 通常取 2 或 3，为可调参数）

Step 4：结合 §2.3 的异常区域先验：
         - 对标记为"限电区域"的数据点：直接剔除而不参与功率曲线建模
         - 对标记为"结冰区域"的数据点：可保留但加标记，供后续状态识别使用
         - 对标记为"正常区域"的异常点：按 KNN 检测结果剔除
```

**（3）参数选择**

- **K 值**：通常取 10~30，通过交叉验证在多个风场上确定最优值；
- **异常判别阈值 n**：取 2（较严格）或 3（宽松），对应不同的清洗力度；
- **特征归一化范围**：风速范围建议以 [0, v_rated] 归一化，密度以 [ρ_min, ρ_max] 归一化。

---

### 2.4.4 清洗效果评估实验

**写作内容：**

**（1）单机清洗效果验证**

- 在同一风机上对比清洗前后的风速-功率散点图和热力图；
- 以 **RMSE（相对于厂商标准功率曲线）** 和 **R²（拟合优度）** 为指标量化清洗效果；
- 分析不同参数（K 值、阈值 n）对清洗结果的敏感性。

**（2）季节性鲁棒性验证**

- 将全年数据按季节（春/夏/秋/冬）拆分，分别评估清洗后功率曲线的一致性；
- 对比以下三种方案的季节性误差：
  - 方案 A：不做任何密度修正的基线
  - 方案 B：仅做 IEC 修正风速的方法
  - 方案 C：本文 (v_corr, ρ) 双特征 KNN 方法

| 方案 | 春 RMSE | 夏 RMSE | 秋 RMSE | 冬 RMSE | 全年均值 |
|------|--------|--------|--------|--------|---------|
| 方案 A | × | × | × | × | × |
| 方案 B | × | × | × | × | × |
| 方案 C | × | × | × | × | × |

**（3）跨风电场泛化性验证**

- 在峡阳A、峡阳B、峡沙、蕴阳等多个风电场独立测试，验证方法的普适性；
- 分析地理位置差异（海拔、气候区）对最优参数的影响。

---

### 2.4.5 清洗数据的质量综合评估

**写作内容：**

经过 §2.1~§2.4 全流程处理后，对输出数据进行综合质量评估：

**评估维度：**
1. **数据完整率**：经各步骤处理后保留的有效数据点比例；
2. **功率曲线光滑度**：清洗后风速-功率曲线的 S 型形态完整性（定性 + 定量）；
3. **季节一致性**：跨季节功率曲线的 KL 散度（衡量密度分布差异）；
4. **预测误差改善**：以第三章的预测模型为下游任务，对比使用清洗前/后数据的预测 RMSE（提前说明本章数据质量对预测精度的直接贡献）。

---

### 2.4.6 本节小结

总结 KNN 双特征清洗方法的核心贡献、适用场景和局限性（如 K 值对稀疏数据区域的敏感性），明确输出数据的质量状态，宣告第二章数据处理任务完成，引出第三、四章的建模工作。

---

## 2.5 本章小结

### 写作要求

本章小结需完成以下任务，建议篇幅 500~800 字：

**（1）逐节总结研究成果**

| 小节 | 解决的问题 | 主要贡献 |
|-----|-----------|---------|
| §2.1 多层级功率结构 | 厘清三层级功率差异来源 | 量化线损规律，建立分析口径规范 |
| §2.2 重复时间戳处理 | 消除时序歧义 | 提出分类去重策略，时序完整性显著提升 |
| §2.3 热力图异常识别 | 自动识别多类功率曲线异常 | 建立热力图异常分类体系，提供精细清洗先验 |
| §2.4 考虑密度的精细清洗 | 消除密度效应和局部异常 | KNN 双特征清洗，跨季节功率曲线一致性显著提升 |

**（2）强调数据质量提升的量化成果**

用 2~3 个关键数字总结：
- 重复时间戳消除率（如 98% 的重复记录被正确分类处理）；
- 功率曲线 RMSE 改善幅度（如相比原始数据降低 X%）；
- 季节性误差改善幅度。

**（3）引出后续章节**

以一段过渡段说明：经本章处理的高质量数据将分别支撑：
- **第三章**：为分层功率预测框架提供精确的单机功率数据和线损知识（来自 §2.1 和 §2.4）；
- **第四章**：为尾流感知场站预测提供可信赖的机组运行状态数据（来自 §2.3 和 §2.4 识别出的异常类型，便于排除尾流遮挡时段的数据偏差）。

---

## 附录：写作建议与章节衔接说明

### A. 推荐的章节结构（含页数参考）

| 节号 | 标题 | 建议页数 | 主要图表数 |
|-----|------|---------|---------|
| 2.0 | 引言 | 1~2 页 | 1 张流程框架图 |
| 2.1 | 多层级功率结构分析 | 8~10 页 | 3~4 张（拓扑图、差异统计图、线损率图） |
| 2.2 | 重复时间戳处理 | 6~8 页 | 2~3 张（时钟漂移示意图、类型分布饼图） |
| 2.3 | 热力图异常识别 | 10~12 页 | 6~8 张（各异常类型热力图 + 对比实验图） |
| 2.4 | 考虑密度的精细清洗 | 10~12 页 | 5~7 张（密度效应图、清洗前后对比图、季节对比图） |
| 2.5 | 本章小结 | 1~2 页 | 0~1 张（综合流程总结图） |
| **合计** | | **36~46 页** | **17~23 张** |

### B. 各节之间的逻辑串联写法

每节开头建议用 1~2 句话"承上启下"：

- **§2.2 开头**："经第 2.1 节分析可知，为保证数据分析口径的统一，本节进一步处理时序数据中存在的重复时间戳问题……"
- **§2.3 开头**："在时间轴歧义消除后，SCADA 数据的主要质量问题转为功率曲线本身的异常形态。本节提出……"
- **§2.4 开头**："第 2.3 节的热力图分析已为各类异常区域提供了位置先验。在此基础上，本节进一步考虑……"
- **§2.5 开头**："本章以风电 SCADA 数据质量提升为核心，沿'层级认知 → 时序整洁 → 粗识别 → 精清洗'的技术路线……"

### C. 数据展示建议

1. **热力图**：统一使用 `viridis` 配色方案，对数归一化颜色轴，分辨率 0.5 m/s × 额定功率的 1%；
2. **误差统计**：建议同时报告 RMSE、MAE 和 NRMSE（归一化 RMSE），方便跨场站对比；
3. **对比实验**：建议使用配对统计检验（Wilcoxon 符号秩检验）验证方法差异的显著性，避免仅凭平均值得出结论。

### D. 与第三、四章的衔接说明

| 第二章输出 | 第三章使用方式 | 第四章使用方式 |
|-----------|-------------|-------------|
| §2.1 线损率查表 | 直接用于 §3.3 集电线路线损修正 | 不直接使用 |
| §2.2 处理后时序数据 | 作为单机预测模型的训练数据 | 作为场站级预测的训练数据 |
| §2.3 异常区域标记 | 排除异常时段，提升单机预测数据质量 | 排除尾流遮挡时段的异常偏差 |
| §2.4 清洗后功率曲线 | 高质量单机功率特征，训练 §3.2 模型 | 高质量场站功率数据，训练 §4.3 模型 |

### E. 创新点提炼建议

本章可提炼的创新点（供第一章绪论和第五章总结使用）：

> **创新点①**：提出了"粗识别 + 精清洗"两步功率曲线数据清洗框架。以风速-功率二维密度热力图自动识别限电截顶、结冰失速、传感器漂移、停机拉尾等多类异常模式，在此基础上以风速和空气密度为双特征维度进行 KNN 局部异常剔除，解决了传统单一阈值方法在多异常并存场景下识别能力不足的问题。

---

*本文档由论文框架文件 [README.md](./README.md) 和研究现状文件 [literature-review.md](./literature-review.md) 配套生成，建议三份文档结合使用。*
